# 🚀 Быстрый Старт - Intersson

## Запуск Проекта в 5 Шагов

### Шаг 1: Установите зависимости Django

Откройте **Terminal** (обычный, не Cursor) и выполните:

```bash
cd ~/Intersson/backend
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 2: Настройте базу данных

```bash
python manage.py makemigrations
python manage.py migrate
python manage.py load_industries
python manage.py createsuperuser
```

При создании суперпользователя введите:
- Username: `admin`
- Email: ваш email
- Password: придумайте пароль

### Шаг 3: Запустите Django (Backend)

```bash
python manage.py runserver
```

✅ **Backend запущен**: http://localhost:8000  
✅ **Админка**: http://localhost:8000/admin

**Оставьте это окно терминала открытым!**

---

### Шаг 4: Запустите Next.js (Frontend)

Откройте **НОВОЕ** окно Terminal:

```bash
cd ~/Intersson
npm run dev
```

✅ **Сайт запущен**: http://localhost:3000

---

## 📋 Что Теперь Доступно

### ✅ Админ-Панель (http://localhost:8000/admin)

#### 1. **Управление Шаблонами (Templates)**
- Выберите индустрию из 10 существующих
- Загрузите PDF шаблон
- Загрузите превью-картинку (опционально)
- Количество шаблонов отображается автоматически

#### 2. **Модерация Отзывов (Reviews)**
- Все отзывы с сайта приходят сюда
- Выберите отзыв(ы) чекбоксом
- Actions → "✓ Approve selected reviews" - одобрить
- Actions → "✗ Reject selected reviews" - отклонить
- Одобренные отзывы сразу появляются на сайте

#### 3. **Редактирование Индустрий (Industries)**
- 10 индустрий уже загружены
- Можно редактировать описание, иконки, градиенты
- Счетчик шаблонов обновляется автоматически

---

### ✅ Сайт (http://localhost:3000)

- **Главная страница**: показывает все 10 индустрий с количеством шаблонов
- **Страница индустрии**: клик на индустрию → список всех PDF шаблонов
- **Страница отзывов** (`/reviews`): 
  - Показывает одобренные отзывы
  - Кнопка "Leave a Review" для отправки нового отзыва
  - Отзыв идет на модерацию в админку

---

## 🎯 Проверка Работы

### Тест 1: Добавить Шаблон
1. Админка → Templates → Add Template
2. Industry: "Business & Services"
3. Name: "Test Template"
4. Загрузите любой PDF
5. Active: ✓
6. Save
7. Откройте http://localhost:3000
8. "Business & Services" покажет "1 template"

### Тест 2: Модерация Отзыва
1. http://localhost:3000/reviews → "Leave a Review"
2. Заполните форму и отправьте
3. Админка → Reviews → увидите новый отзыв (Pending)
4. Выберите его → Actions → Approve
5. Обновите страницу отзывов - отзыв появился!

---

## 📁 Структура Проекта

```
Intersson/
├── src/               # Next.js фронтенд
├── backend/           # Django бэкенд
│   ├── api/          # Модели, админка, API
│   ├── media/        # Загруженные файлы (PDF, картинки)
│   └── db.sqlite3    # База данных
└── SETUP_INSTRUCTIONS.md  # Подробная документация
```

---

## 🔧 Полезные Команды

### Backend
```bash
cd ~/Intersson/backend
source venv/bin/activate

# Запуск сервера
python manage.py runserver

# Загрузить индустрии заново
python manage.py load_industries

# Создать нового админа
python manage.py createsuperuser
```

### Frontend
```bash
cd ~/Intersson

# Запуск dev сервера
npm run dev

# Если порт 3000 занят
PORT=3001 npm run dev
```

---

## ❓ Проблемы?

### Backend не запускается
```bash
cd ~/Intersson/backend
source venv/bin/activate
pip install -r requirements.txt
```

### Frontend показывает "Cannot connect to API"
1. Проверьте, что Django запущен (http://localhost:8000)
2. Проверьте файл `.env.local` в корне проекта

### Порт занят
**Django (8000):**
```bash
python manage.py runserver 8001
# Обновите .env.local: NEXT_PUBLIC_API_URL=http://localhost:8001/api
```

**Next.js (3000):**
```bash
PORT=3001 npm run dev
```

---

## 📚 Документация

- **SETUP_INSTRUCTIONS.md** - полная инструкция
- **backend/README.md** - документация Django API

---

## 🎉 Готово!

**Админка**: http://localhost:8000/admin  
**Сайт**: http://localhost:3000  
**API**: http://localhost:8000/api/

Можете начинать добавлять шаблоны и модерировать отзывы! 🚀
